# NEXT STEPS

1. Add table-driven tests for every `entry-*` path validation code (`entry-absolute`, `entry-backslash`, `entry-url`, `entry-directory`, etc.).
2. Add a dedicated validator test matrix for profile-specific entry compatibility across `general-document`, `comic`, and `storyboard`.
3. Add CLI integration tests that assert machine-readable issue codes are emitted unchanged by CLI formatting.
